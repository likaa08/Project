# Проверка: проект на оценку 5 (10 баллов)

## Критерии Moodle (техника C#)

| Требование | Выполнено | Где в проекте |
|------------|-----------|----------------|
| ≥ 2 интерфейса | ✅ | `ICountable`, `IFilter`, `IChangeable` — `Model/Core/Interfaces/` |
| ≥ 2 абстрактных класса | ✅ | `Pet`, `SerializerBase<T>` — `Model/Core/Pet.cs`, `Model/Data/SerializerBase.cs` |
| > 5 приведений к базовому классу | ✅ | `MainForm.GetFilteredPets` — `Pet asPet = pet`, `result.Add(asPet)` в цикле |
| > 5 приведений к интерфейсу | ✅ | `IFilter`, `ICountable` в `MainForm`; `IChangeable` в `PetsTableForm`; реализации в `Shelter.*.cs` |
| > 1 перегрузка методов | ✅ | `Count()` / `Count(Type)`; `Filter(Type?)` / `Filter(Type?, bool)` |
| > 3 переопределения методов/свойств | ✅ | `AnimalKind`, `GetDescription`, `Title` у Cat/Dog/Rabbit/Parrot; `ToString`, `Equals` у `Pet` |
| ≥ 1 перегрузка оператора | ✅ | `Pet.operator ==`, `operator !=` |
| ≥ 1 делегат | ✅ | `Predicate<Pet>` в `Shelter.Filter`; `Func<string,string>` в `MainForm.btnShow_Click` |
| > 1 обобщённый тип | ✅ | `SerializerBase<T>`, `JsonSerializerService<T>`, `XmlSerializerService<T>`, `PetList<T>` |
| > 3 элемента UI | ✅ | ComboBox, Label, Button, CheckBox, DataGridView, TextBox, NumericUpDown |

## Базовая часть (5 баллов)

| Пункт | ✅ | Файл |
|-------|---|------|
| Класс `Pet` | ✅ | `Model/Core/Pet.cs` |
| Cat, Dog, Rabbit (+ поля) | ✅ | `Cat.cs`, `Dog.cs`, `Rabbit.cs` |
| Parrot (доп. вид) | ✅ | `Parrot.cs` |
| `ICountable` с перегрузками | ✅ | `ICountable.cs`, `Shelter.Countable.cs` |
| `IFilter` | ✅ | `IFilter.cs`, `Shelter.Filter.cs` |
| `Shelter` | ✅ | `Shelter.cs` |
| 10–20 питомцев, 3+ приюта | ✅ | `SampleData.cs` (23 питомца, 3 приюта) |
| Меню, фильтры, таблица | ✅ | `MainForm`, `PetsTableForm` |
| JSON приютов | ✅ | `ShelterRepository.cs` |
| Подборка в файл | ✅ | `ReportService.SaveSelection` → `Подборка_№N_от_дата` |

## Дополнительная часть (3 балла)

| Пункт | ✅ | Файл |
|-------|---|------|
| Библиотека Model | ✅ | `Model/Model.csproj` |
| partial `Pet` + клаустрофобия | ✅ | `Pet.cs`, `Pet.Claustrophobia.cs` |
| partial у наследников | ✅ | `*.Claustrophobia.cs` |
| Кошка: клаустрофобия по умолчанию true | ✅ | `Cat.cs` |
| Собака: по умолчанию false | ✅ | `Dog.cs` |
| Закрытый приют — нельзя с клаустрофобией | ✅ | `Shelter.CanPlacePet` |
| `Filter(Type, bool)` | ✅ | `Shelter.Filter.cs` |
| Галочка открытой территории | ✅ | `MainForm` — `chkOpenAreaOnly` |
| Колонка клаустрофобии | ✅ | `PetsTableForm` — таблица |

## Продвинутая часть (2 балла)

| Пункт | ✅ | Файл |
|-------|---|------|
| Папки Core и Data | ✅ | `Model/Core`, `Model/Data` |
| Абстрактная сериализация + JSON/XML | ✅ | `SerializerBase`, `JsonSerializerService`, `XmlSerializerService` |
| `IChangeable` | ✅ | `IChangeable.cs`, `Shelter.Changeable.cs` |
| Добавление/удаление в таблице | ✅ | `PetsTableForm` |
| Файлы json/xml для нового питомца | ✅ | `ReportService.SavePetToReportsIfNew` |
| Выбор формата отчёта + копирование | ✅ | `MainForm.cmbFormat`, `ConvertAllReports` |

## ООП и SOLID (для устной защиты)

- **Инкапсуляция** — поля приюта `_pets`, проверки в `CanPlacePet`, `TryAddPet`.
- **Наследование** — Cat/Dog/Rabbit/Parrot → Pet.
- **Полиморфизм** — `AnimalKind`, `GetDescription`, `Filter` по типу.
- **Абстракция** — `Pet`, `SerializerBase<T>`, интерфейсы.
- **SRP** — Model = логика, PetShelter = UI, Data = файлы.
- **OCP** — новый вид через класс + ветка в `PetFactory`.
- **LSP** — наследники подставляются вместо `Pet`.
- **ISP** — отдельные `ICountable`, `IFilter`, `IChangeable`.
- **DIP** — UI зависит от интерфейсов и Model, не от JSON напрямую.

## Перед сдачей

1. `dotnet build` — без ошибок.
2. `dotnet run --project PetShelter` — меню, фильтры, таблица, добавление/удаление.
3. Проверить `%LocalAppData%\PetShelter\` — `shelters.json` и папка `Reports`.
